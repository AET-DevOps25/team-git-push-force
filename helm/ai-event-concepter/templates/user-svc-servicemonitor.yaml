apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-svc-servicemonitor
  namespace: team-git-push-force-monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: user-svc
      monitoring: "true"
  namespaceSelector:
    matchNames:
      - team-git-push-force
  endpoints:
    - port: user-svc
      path: /actuator/prometheus
      interval: 15s 