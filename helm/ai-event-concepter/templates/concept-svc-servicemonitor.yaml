apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: concept-svc-servicemonitor
  namespace: team-git-push-force-monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: concept-svc
      monitoring: "true"
  namespaceSelector:
    matchNames:
      - team-git-push-force
  endpoints:
    - port: concept-svc
      path: /actuator/prometheus
      interval: 15s 